{
  "name": "friday-mcp",
  "version": "1.0.0",
  "description": "MCP server wrapper for FRIDAY/SPES TextToSQL webhook",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
